<div class="container mt-5 min-vh-100">
  <div class="container">
    <h1 class="h3 fw-bold">Your listed spaceships</h1>
  <ul>
    <% @ships.each do |ship| %>
      <li><%= link_to ship.name, ship_path(ship.id) %></li>
    <% end %>
  </ul>
  </div>
</div>
